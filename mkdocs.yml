site_name: Cuems Editor
repo_url: https://github.com/stagesoft/cuems-editor
theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          paths: [src]

markdown_extensions:
  - admonition
